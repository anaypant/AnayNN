import numpy
import pygame

class Module:
    def __init__(self):
        # has a shape, layers, etc.
        self.__shape = []
    
    def add(self, layer):
        # adds a layer to the model -- can be any type of layer for now
        pass